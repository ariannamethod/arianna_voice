{
  "providers": {
    "openai": {
      "models": ["gpt-4o"],
      "api_key": "${OPENAI_API_KEY}"
    },
    "gemini": {
      "models": ["gemini-pro"],
      "api_key": "${GEMINI_API_KEY}"
    },
    "grok": {
      "models": ["grok-3-mini"],
      "api_key": "${XAI_API_KEY}"
    }
  },
  "fallback_order": ["openai","gemini","grok"]
}
